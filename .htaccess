# deny access to all files in the directory
Order deny,allow
Deny from all

# allow access only to index.html
<Files "presence.json">
    Order allow,deny
    Allow from all
</Files>

# allow access to all files in the directory /modules/public
<Directory "/modules/public">
    Order allow,deny
    Allow from all
</Directory>